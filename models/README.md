# Saved models

Place generated trained model artifacts (for example `.joblib`, `.pkl`, `.h5`, or `.keras` files) here.

The supplied XGBoost notebook contains code to save trained models. The original ZIP did not contain the generated model binary files, so they have not been fabricated or added here.
